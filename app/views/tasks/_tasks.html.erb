<% unless tasks.nil? %>
  <ul class="list-checklist">
    <% tasks.each do |task| %>
      <li><%= check_box_tag :task, "",task.status, :title => "#{task.name}", :onclick => "TaskStatusUpdate(this, #{task.id}, #{@transaction.id})" %><%= task.name %></li>
    <% end %>
  </ul>
<% end %>